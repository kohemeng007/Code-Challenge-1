function removeDuplicates(arr){
    const uniqueItems = [];

    arr.forEach(item => {
        if(!uniqueItems.includes(items)) {
            uniqueItems.push(item);
        }
    });

    return uniqueItems; 

} 